# A0 复核 PR #3：R1 / R2 关闭，整体继续等待 Unity 环境

日期：2026-09-20。审阅者：A0。对象：[PR #3](https://github.com/L1nkkkk/ai-companion/pull/3)，head `b9eb29763a97fb5259cb2e3ffb0214cf7194a7f5`。修复实现及开发者干净实测提交：`7f905b5d0a027844cf45d554558cc6440e21a2a7`；该提交到当前 head 只增加或修改报告。

**本轮关闭首轮的 U01-00-R1、U01-00-R2。U01-00 整体仍为 `awaiting_external`，UA01、UA02 未通过；没有放行后续实现、冻结兼容版本或批准合并。** [首轮记录](PR-003-review.md)保留为历史，本页覆盖其中两项返工的当前状态；[结构化验收状态](PR-003-round2-verification.json)明确区分测试工具通过与产品待验证。

后续设计补充：用户授权更换版本，经独立实验，[ADR16](../../../adr/0016-unity-2022-r41-fallback-validation.md)已允许原 owner 将 Unity 2022.3 / R4_1 / BiRP 迁入正式工程并验证。U01-00 当前恢复 `in_progress`；本页记录原 R5 提交的审阅事实，两项关闭结论保持不变。下文对 Unity 6 的单一路径要求由 ADR16 覆盖；新候选尚未通过正式验收。

## 1. R1：UI 接口设计补齐，通过

已审阅[修订接口提案](https://github.com/L1nkkkk/ai-companion/blob/b9eb29763a97fb5259cb2e3ffb0214cf7194a7f5/docs/reports/U01/U01-00/interfaces-proposal.md)。Session 已提供即时音量、设置/容量快照、音色状态、麦克风枚举、有界历史分页和单会话导出；History 与 Capture 对应入口、数据类型、线程、取消、错误和容量约束齐全，UI 调用样例不绕过 Session。

已核对的边界包括：不因查询历史停止当前播放；导出捕获点击时的会话 ID；分页绑定存储修订；无设备与枚举错误区分；只改自动朗读时允许保留暂不可用的旧设备/音色偏好，真正使用前重新校验。此项认可接口提案的补齐，**不等于共享 C# 类型已编译、对应功能已实现或整个共享 API 最终冻结**。实际 Contracts、默认设置和包组合仍由集成 owner 在后续正式冻结提交中统一提供。

## 2. R2：Player 测试外部超时，通过

已审阅[修订测试脚本](https://github.com/L1nkkkk/ai-companion/blob/b9eb29763a97fb5259cb2e3ffb0214cf7194a7f5/tools/unity/Test-Player.ps1)：以独立墙钟限制进程等待；超时只终止本次持有的 Process，再等待最多 5 秒；返回 124 并保留失败状态、日志和文件哈希。缺失画面或错误结果返回非零，正常证据校验仍保留。

A0 独立执行了开发者提供的四类测试，使用哈希匹配的已有 ControlledPlayer 测试程序，无需编译或安装 Unity。临时副本与结果写在设计工作树的忽略目录内，开发工作树保持干净。结果见 [A0 受控进程复测](PR-003-round2-harness.json)：

| 用例 | wrapper 退出码 | 应用等待耗时 | 判定 |
|---|---:|---:|---|
| 正常退出 | 0 | 15.133 秒 | 通过；没有触发终止 |
| 无限挂起 | 124 | 16.021 秒 | 通过；仅终止本次进程，独立同映像哨兵仍存活 |
| 缺失图片 | 1 | 0.303 秒 | 通过；拒绝不完整证据 |
| 错误结果 | 1 | 0.090 秒 | 通过；拒绝无效结果 |

四次输出哈希均通过。另已将开发者报告中的三个源码哈希、已有测试程序哈希及四份原始 runtime manifest / 证据哈希与实际文件核对一致。以上测试只证明测试工具行为；合成图片不是 Unity 画面，耗时不是产品首播或帧率数据。

## 3. 保留的环境与验收状态

开发者仍未取得 Unity 6.3；新增的官方 `6000.3.21f1` 下载失败记录已在环境报告中登记。A0 本轮没有重复网络排障或安装。当前 PR 的 [Foundation CI](https://github.com/L1nkkkk/ai-companion/actions/runs/35501508354) 成功，但不包含 Unity 构建。

Windows Player 包、实际导入/编译日志、Editor 生成的依赖锁、URP 材质/遮罩/排序画面与运行证据仍缺失。UA01/UA02 继续 `awaiting_external`，UA03–UA12 继续 `not_run`。两个审阅项关闭不替代这些验收。

## 4. 原开发会话的下一步

不重复修复已关闭的 R1 / R2。先取得可用的官方 Unity 6.3 LTS Windows Editor 或安装器路径，并确认构建组件与许可。`6000.3.11f1` 是候选；其他正式 6.3 补丁仍可按首轮决定提交依据并统一实测。

环境具备后，完成实际导入/编译、生成并审阅依赖锁和工程设置、干净提交构建 Windows ZIP 及 SHA-256、同包启动与画面/运行证据，再更新 PR #3 交 QA/A0。依赖任务继续等待同一经过验收的工程起点。

可复制续作指令：

```text
A0 已在 b9eb29763a97fb5259cb2e3ffb0214cf7194a7f5 关闭 U01-00-R1/R2，完整记录见设计分支 docs/reports/U01/acceptance/PR-003-round2-review.md。
继续原 PR #3；整体仍 awaiting_external，后续任务未放行。
下一步先核对是否已取得可用的官方 Unity 6.3 Editor/安装器及相应构建与许可条件；已有相同 404 结果无需反复重试。
条件具备后，完成目标 Editor 导入/编译、真实依赖锁、干净提交 Windows 构建、完整包和 SHA-256、实际启动画面与运行证据，再交 A0；不以受控测试程序或基础 CI 替代 Unity 验收。
```
