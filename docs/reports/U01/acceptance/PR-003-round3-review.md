# A0 验收 PR #3：U01-00 通过，冻结基础工程并放行模块开发

日期：2026-09-20。审阅者：A0。[PR #3](https://github.com/L1nkkkk/ai-companion/pull/3) head 为 `cfffbf6b307e58421d0316cda67d027a5f39be7d`；实际构建/运行源码为 `849c6c982d875f477d88933af4abea5a163d0fa6`，包含设计提交 `000f2b7`。A0 确认两者间 40 个变更文件全部属于该任务报告，产品代码、工具和资源清单没有变化。

**U01-00 的工程、可复现构建与基础渲染范围通过。冻结以下版本和 C# 边界，放行 U01-01、02、03、04。** UA01 接受；UA02 的基础子项接受，完整 UA02 仍待角色实现与验证。U-G0、U-G1、U-G2 和 U01 整体均未通过。本轮不执行 PR 合并或启动新的开发会话。

## 固定基线

| 项目 | 通过本次范围的版本 / 约定 |
|---|---|
| Unity | `2022.3.62f3c1 / 1623fc0bbb97`，Windows x64 / Mono / D3D11 |
| Live2D | 完整官方 Cubism `5-r.4.1`、同包 Core `5.1.0`、Mao；Built-in 管线 |
| UI 依赖 | uGUI `1.0.0`、TMP `3.0.6`；Noto Sans CJK SC `2.004`，字体可用不代表 IME 已通过 |
| 共享边界 | 源码 `849c6c9` 的 Companion.Contracts，六接口、typed DTO/事件、身份和 PCM 生命周期；[冻结及验证器注入决定](../../../tasks/U01/CSHARP-BASELINE.md) |
| 后续代码起点 | 推荐 `cfffbf6`（含被测代码与报告），再合入包含本验收和 CSHARP-BASELINE 的设计提交；记录两个精确 SHA。不能从旧 main 或 P01 网页分支起步 |

这个冻结只针对已测 Windows 开发原型。没有推导第二台机器、Mac、手机后台、直播或任意模型兼容性。Core/SDK/模型按资源说明获取，生成文件变化与原包字节区别保留。

## 独立复核与实际结果

A0 阅读固定代码、Contracts、版本/恢复/构建改动及报告，并只读核验已有产物；没有在本会话安装、重建或再次运行 Unity。复现 worktree 当前仍位于 `849c6c9`，Git 干净；首次无 Library、空下载缓存、构建前后源码干净的时间点以开发记录为据。

独立结果见 [结构化复核](PR-003-round3-verification.json)：

- 构建/运行清单一致，**210 个完整程序文件**的实际目录与 ZIP 长度、SHA-256 全部匹配；不是仅核对启动 exe。
- **27 份原始运行证据**哈希匹配；便携证据 ZIP 的 **39 项**及已入库的 **26 项副本**匹配。原始日志与 UTF-8 LF 副本分开核验，没有把两者的哈希混用。
- 同包 Player 运行 **600.025 秒**，外部监督退出 0、未触发超时；运行 errors/warnings 均为 0。构建另有两项已登记警告，不能称为零构建警告。
- 独立重算 **35,811 帧**：P95 **17.4517 ms**、最大 **64.1001 ms**，**99.9860% ≤33.3 ms**，无 ≥1000 ms 样本。排除启动前 3 秒；它是 Unity Update 间隔，不是 GPU 时间或真实 UI 输入延迟。
- 核对 **10 轮、190 个唯一阶段样本**和 **20 次非循环动作**的完成及 begin/end 标志。Core 参数/几何与 SDK 动作证据支持基础呈现，不证明用户交互已经实现。

A0 亲自查看中性、张嘴、左右单眼/双眼闭合、左右视线、TapBody 和 special_01 共 **9 张实际 PNG**。所看画面未发现明显粉色缺材质、闭眼虹膜泄漏或明显错层；特效靠近上沿并重叠测试文字。静帧不能证明连续呼吸、所有过渡及完整遮罩路径。

开发资源审计记录 1022 个恢复文件匹配、4 个编译响应文件及 meta 排除、34 个模型序列化资源变化、SDK C#/Shader/Core 未变、606 个 meta GUID 唯一。A0 审阅其来源与分类，不将重序列化文件的哈希差异等同语义已经逐项证明。

正式程序 ZIP SHA-256：`8c59e9c54cb89e5dafac2027fc8bdfa1ce53bd0c5a93f889435d15361bd8a6db`。
便携证据 ZIP SHA-256：`47ad4ef2863297772b21334a4a11dc0306ee376e89a93ba3c7600ace5510fc7f`。
获取位置和原始记录见[开发交接](https://github.com/L1nkkkk/ai-companion/blob/cfffbf6b307e58421d0316cda67d027a5f39be7d/docs/reports/U01/U01-00/report.md)。

## 限制、责任与后续门槛

| 编号 | 发现 / 不能宣称的结论 | owner 与完成条件 |
|---|---|---|
| U01-F01 | 工作集约 30 秒基线 **251.89 MiB**，结束 **462.88 MiB**，增长 **210.98 MiB**。没有时间曲线，不能判断是预热/截图/缓存还是持续增长，更不能宣称无泄漏 | **U01-01 主责，U01-05 测量接线，QA 复核；U-G0 前完成定位记录。** 在固定包/相同动作下采集每 5 秒工作集、私有内存与 Unity 分配曲线，标记资源加载、截图与模型释放；比较关闭截图的重复动作运行，观察完整预热后的多轮变化。若持续未收敛，先定位修复；稳定缓存则以曲线和资源上界说明。UA11 再做业务长时回归 |
| U01-F02 | Core 可见联集只覆盖 **32/37 masked、8/10 inverted**；未观察 masked：ArtMesh274/273/256/193/192，inverted：ArtMesh193/192 | **U01-01 + QA；完整 UA02 前处理。** 定位对应状态，补充路径与实图；不进入产品范围的路径明确禁用或降级并登记，不能把不可见标志写成所有遮罩通过 |
| U01-F03 | special_01 外缘接近上沿、遮挡测试标签；不能证明所有特效完整可见 | **U01-01/02，U01-05 集成；U-G0 前处理。** 产品镜头与 UI 覆盖实际会用到的动作包络、窗口尺寸与必要控件，不沿用测试标签布局 |
| U01-F04 | 仅同机干净 worktree；完整精确 Editor 安装器的取得/哈希与第二机器复现未完成 | **T00 第二机器 owner / QA，在正式多机交接前补齐。** 记录官方安装器身份、模块、哈希及新机导入构建；本次 Windows 起点不声称 Mac 或手机已验证 |

这些后续项不否定本次“能按源码构建并稳定完成基础渲染测试”的工程证据，故不阻止模块开发。内存增长、未覆盖遮罩等也不会因 U01-00 完成而自动关闭。自然眨眼、鼠标跟随、用户招呼、缺参数降级归 U01-01；实际音频口型与取消归 U01-04/01；UI、IME、历史及云服务仍按原 UA 验收。R1/R2 保持关闭。

## 下一步

版本和共享类型可供四个模块共同使用，默认先推进 **U-G0：可互动角色 + 窗口控件 + 测试声音及真实口型/停止**。U01-03 同步完成预览 schema/固定样例与后台接口；缺云密钥不会阻塞 G0，真实云能力不提前宣布通过。

各 owner 按[派工说明](../../../tasks/U01/DISPATCH.md)独立工作，集成 owner 统一改总场景、依赖锁和 Composition。U01-00 到此关闭；后续阶段的新问题按上述责任处理，不反复重做已完成的版本试验。
