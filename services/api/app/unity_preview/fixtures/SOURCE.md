# 随包测试 WAV 来源

文件：`demo-tone.wav`。2026-09-21 为本项目原创生成的数学正弦测试信号；生成源是同目录 `generate_audio.py`，不含人声、个人录音、第三方声音素材或模型输出。该测试素材及生成脚本以 CC0-1.0 提供，可随项目复制、修改和分发。它用于实际音频输出、音量、静音、停止和口型振幅验证，不作为 TTS 朗读证据。

- RIFF/WAVE PCM16 little-endian，单声道，24000 Hz。
- 时长 8 秒，共 192000 个样本，文件长度 384056 字节。
- 包含合法的 3 字节 `JUNK` chunk 和补齐字节，用于验证消费者不会假定 44 字节固定头。
- SHA-256：`230b4f63904de05c821a39248a5496399db8d700c72385e7b8479d82bfca3540`。

| 时间（秒） | 信号 |
|---|---|
| 0–0.5 | 静音 |
| 0.5–2 | 220 Hz，最大振幅 0.22，30 ms 淡入淡出 |
| 2–3 | 静音 |
| 3–5 | 330 Hz，同振幅 |
| 5–6 | 静音 |
| 6–7.5 | 440 Hz，同振幅 |
| 7.5–8 | 静音 |

生成命令（仓库根目录）：`.venv/Scripts/python.exe services/api/app/unity_preview/fixtures/generate_audio.py`。测试逐字节核对重新生成的内容与随包文件，哈希仅描述素材，不宣称扬声器或口型已通过真机验收。
