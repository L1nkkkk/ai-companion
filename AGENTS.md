# AI 虚拟角色项目协作指令

本目录为独立正式开发仓库。架构起点由 A0 管理，任务台账位于 docs/blueprint/planning/tasks.json。

## 当前有效方向与会话分工

用户已明确：客户端按 Unity / C# + Cubism SDK for Unity 设计，先做 Windows 桌面原型。先读 [ADR15](docs/adr/0015-unity-client-and-design-only-a0.md) 和 [U01 任务书](docs/tasks/U01/TASKBOOK.md)；U01 台账位于 docs/blueprint/planning/unity-desktop.json。旧 React / React Native 工程保留为历史起点，P01 网页仅作交互参考。

**A0 所在设计会话只负责设计、派工文档、评审和验收。** 可修改设计/协作文档、设计台账，运行只读检查及验收已有产物；不得在该会话创建产品工程、安装 SDK、实现业务、修改构建配置、修复模块或代替开发 owner 打包。发现问题写明复现与验收条件，交独立开发会话处理。用户未要求启动开发会话时，编写派工提示不等于创建或启动任务。

明确领取实现任务的开发会话可在授权目录自主开发、自测与修复；上述限制针对 A0 设计会话，不阻塞已分配的开发 owner。集成代码和共享配置由指定的开发集成 owner 落地，A0 负责设计审阅与最终验收结论。

## 项目目标

实现桌面语音聊天、一个真实直播平台的弹幕互动，以及 Android 和 iOS 独立 App。手机支持用户主动开启的后台陪伴语音；灵动岛、锁屏实时活动和 Android 持续通知负责状态和快捷操作。AI 初期使用云端服务。

## 开始前阅读

阅读 docs/blueprint/README.md、docs/blueprint/ARCHITECTURE.md、docs/blueprint/CONTRACTS.md、自己任务及对应验收条目。以用户最新指示和已接受 ADR 为准。不要依赖过往聊天里未写入文档的推测。

## 分工与工作范围

按任务 ID 在独立分支或 worktree 开发，只修改分配目录。开发 owner 可以自主完成范围内常规实现、修复与测试。A0 负责跨模块接口、状态机、目录边界和集成方案评审；指定开发 owner 实施集成。多个开发 agent 可以在互不冲突的任务上并行，实际派工以用户启动的开发会话为准。

## 必须保持的行为

- 后台音频、连接和凭据续期归手机原生引擎；不能依靠暂停中的 Unity 主循环、WebView 或 JS 定时器。
- 停止操作先本机静音，然后取消服务端；旧 turn 和 epoch 的音频、字幕与动作丢弃。
- 每个 session 只有一个输入设备和一个播放设备；接管必须显式。
- 个人和直播上下文及记忆隔离；显示端不能获得控制者权限。
- 云端密钥由服务端保管；麦克风静音后停止上传，结束后停止录音。
- 原始录音不默认持久化；历史与记忆支持删除。
- 不把 mock、模拟器或静态截图当作真实手机后台或直播接入验证。
- 不使用伪造通话、循环静音音频或绕过平台权限的方法掩盖能力缺口。

## 契约与共享文件

contracts 下的 schema、OpenAPI、固定样例和对应文档一起维护。修改需要 A0 统一审阅消费者影响。根依赖锁文件、迁移顺序和原生工程公共配置登记 owner 后再改。

## 完成交付

提供实现、相关测试、实测环境、复现步骤、已知限制、契约变更和交接报告。任务完成以验收证据和集成为准。资源缺失时先完成独立工作，准确报告缺口。

## 文件和资源

不要提交密钥、完整日志中的私人内容、运行数据库、缓存和构建临时目录。模型与 SDK 按资源清单准备，保留来源和获取说明。若仓库旁存在 ChatGPT 同步 sources 目录，将其视为只读参考。

## 本地验证入口

先按 README 安装冻结依赖，再运行 uv run python tools/check.py。未经 A0 更新决策和契约清单，不修改 contracts。原生构建证据在对应设备报告中登记。
