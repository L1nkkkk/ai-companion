# T00 does not load cloud credentials or connect to a database.
# T05/T06 own the provider/database settings and loading implementation.
AIC_ENV=development
AIC_HOST=127.0.0.1
AIC_PORT=8000
AIC_RAW_AUDIO_PERSISTENCE=false
# Never use VITE_ variables for secrets: those are public browser assets.
